FILE(GLOB DS_SOURCES *.h *.cpp)

INCLUDE_DIRECTORIES(${MPI_INCLUDE_PATH})
SET(DS_LIBS ${DS_LIBS} ${MPI_LIBRARIES})

ADD_LIBRARY(ds "${DS_SOURCES}")

TARGET_LINK_LIBRARIES(ds m ${DS_LIBS})
SET_TARGET_PROPERTIES(ds PROPERTIES LINKER_LANGUAGE CXX)
